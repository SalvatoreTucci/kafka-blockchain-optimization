# Factorial Design Configuration Template
# Copy and modify for custom factor combinations

# === FACTORIAL FACTORS ===
# Factor A: Batch Size (bytes)
KAFKA_BATCH_SIZE=${BATCH_SIZE}           # 16384 (low) or 65536 (high)

# Factor B: Linger Time (ms)  
KAFKA_LINGER_MS=${LINGER_MS}             # 0 (low) or 10 (high)

# Factor C: Compression Type
KAFKA_COMPRESSION_TYPE=${COMPRESSION}    # none (low) or lz4 (high)

# === FIXED PARAMETERS ===
# Buffer and Network Settings
KAFKA_BUFFER_MEMORY=67108864             # 64MB buffer
KAFKA_NUM_NETWORK_THREADS=4              # Balanced for factorial testing
KAFKA_NUM_IO_THREADS=8                   # Standard I/O threads

# Socket Buffers
KAFKA_SOCKET_SEND_BUFFER_BYTES=131072    # 128KB send buffer
KAFKA_SOCKET_RECEIVE_BUFFER_BYTES=131072 # 128KB receive buffer

# Replica Settings
KAFKA_REPLICA_FETCH_MAX_BYTES=2097152    # 2MB replica fetch

# Test Configuration
TEST_NAME=${CONFIG_NAME}                 # Configuration identifier
TEST_DURATION=300                        # 5 minutes per test
TEST_TPS=1000                           # Target transactions per second

# === USAGE EXAMPLES ===
# Low-Low-Low (config_000):   BATCH_SIZE=16384, LINGER_MS=0, COMPRESSION=none
# Low-Low-High (config_001):  BATCH_SIZE=16384, LINGER_MS=0, COMPRESSION=lz4
# Low-High-Low (config_010):  BATCH_SIZE=16384, LINGER_MS=10, COMPRESSION=none
# Low-High-High (config_011): BATCH_SIZE=16384, LINGER_MS=10, COMPRESSION=lz4
# High-Low-Low (config_100):  BATCH_SIZE=65536, LINGER_MS=0, COMPRESSION=none
# High-Low-High (config_101): BATCH_SIZE=65536, LINGER_MS=0, COMPRESSION=lz4
# High-High-Low (config_110): BATCH_SIZE=65536, LINGER_MS=10, COMPRESSION=none
# High-High-High (config_111):BATCH_SIZE=65536, LINGER_MS=10, COMPRESSION=lz4
